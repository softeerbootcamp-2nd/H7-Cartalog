import styled from 'styled-components';

export const Pick = styled.div`
  height: 720px;
  overflow: hidden;
`;

export const PickModel = styled.div`
  display: flex;
  margin-top: 8px;
  gap: 16px;
`;
